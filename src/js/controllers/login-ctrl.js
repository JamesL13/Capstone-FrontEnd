/**
 * Login Controller
 */

angular.module('Songs').controller('LoginCtrl', ['$scope', '$http', '$cookieStore', CreateAccountCtrl]);

function LoginCtrl($scope, $http, $cookieStore) {
    var server = 'http://thomasscully.com';
    
    var init = function() {


    }
    init();
}
